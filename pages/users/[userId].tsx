import Layout from "@components/layout";
import UserTemplate from "@components/user-template";

const User = () => {
  return (
    <Layout canGoBack>
      <UserTemplate />
    </Layout>
  );
};

export default User;
